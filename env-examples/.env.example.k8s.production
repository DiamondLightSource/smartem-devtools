# SmartEM Decisions - Production Environment (K8s)
# Copy this file to .env.k8s.production and replace placeholder values with real credentials
# Used by: ./tools/k8s/dev-k8s.sh when DEPLOY_ENV=production

# Docker/GHCR credentials for container registry access
DOCKER_USERNAME=your_github_username
DOCKER_EMAIL=your_email@example.com
DOCKER_PASSWORD=your_github_token_or_use_gh_auth_token  # pragma: allowlist secret

# Database configuration (used for K8s ConfigMap and Secrets)
POSTGRES_HOST=postgres-service
POSTGRES_PORT=5432
POSTGRES_DB=smartem_db
POSTGRES_USER=your_postgres_username
POSTGRES_PASSWORD=your_secure_postgres_password  # pragma: allowlist secret

# RabbitMQ configuration (used for K8s ConfigMap and Secrets)
RABBITMQ_HOST=rabbitmq-service
RABBITMQ_PORT=5672
RABBITMQ_USER=your_rabbitmq_username
RABBITMQ_PASSWORD=your_secure_rabbitmq_password  # pragma: allowlist secret

# HTTP API configuration
HTTP_API_PORT=8000
ADMINER_PORT=8080

# CORS Configuration (comma-separated list of allowed origins)
# For production, ONLY specify exact trusted origins
# Example: "https://app.example.com,https://admin.example.com"
CORS_ALLOWED_ORIGINS=https://app.example.com
