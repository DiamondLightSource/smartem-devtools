{
  "private": true,
  "scripts": {
    "labels:check": "npx tsx scripts/github/sync-labels.ts --check",
    "labels:sync": "npx tsx scripts/github/sync-labels.ts --sync"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "zx": "^8.8.5"
  }
}
